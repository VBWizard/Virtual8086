using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Runtime.InteropServices;
using System.Globalization;

namespace VirtualProcessor
{
/*    class PCIHostController : cPortHandler
    {
        PCIHostController()
        {
            mDeviceName = "PCI Host Controller";
        }

        private TypeCode PortSize;

        public override PortInfo[] PortNums
        {
            get { return mHandledPorts; }
        }
        public override void HandleOut(cValue Port, cValue Value)
        {
        }
        public override void HandleIn(cValue Port, cValue Value)
        {
        }
    }
    class CMOSRAMRTCController : cPortHandler
    {
        CMOSRAMRTCController()
        {
            mDeviceName = "CMOS RAM/RTC Controller";
            //mHandledPorts = new PortInfo[5] {new PortInfo(70, TypeCode.Byte)}
            PortInfo p = new PortInfo(70, TypeCode.Byte);

            byte[] test = new byte[5] {1,2,3,4,5};
        }

        private TypeCode PortSize;

        public override PortInfo[] PortNums
        {
            get { return mHandledPorts; }
        }

        public override void HandleOut(cValue Port, cValue Value)
        {
        }
        public override void HandleIn(cValue Port, cValue Value)
        {
        }
    }
*/ 
    }
